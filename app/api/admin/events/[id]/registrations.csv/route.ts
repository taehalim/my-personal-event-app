import { getAdmin } from '@/lib/auth';
import { createAdminClient } from '@/lib/supabase/admin';
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){if(!await getAdmin())return new Response('Unauthorized',{status:401});const {id}=await params;const {data}=await createAdminClient().from('registrations').select('name,email,status,consent_at,registered_at,cancelled_at,answers').eq('event_id',id).order('registered_at');const rows=[['name','email','status','consent_at','registered_at','cancelled_at','answers'],...(data??[]).map(r=>[r.name,r.email,r.status,r.consent_at,r.registered_at,r.cancelled_at??'',JSON.stringify(r.answers)])];const csv='\ufeff'+rows.map(row=>row.map(value=>`"${String(value).replaceAll('"','""')}"`).join(',')).join('\r\n');return new Response(csv,{status:200,headers:{'Content-Type':'text/csv; charset=utf-8','Content-Disposition':`attachment; filename="registrations-${id}.csv"`}});}
