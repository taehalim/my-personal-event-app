import { NextResponse } from 'next/server';
import { getAdmin } from '@/lib/auth';
import { createAdminClient } from '@/lib/supabase/admin';
export async function POST(request:Request){if(!await getAdmin())return NextResponse.json({error:{code:'UNAUTHORIZED',message:'로그인이 필요합니다.'}},{status:401});const form=await request.formData();const file=form.get('file');const eventId=form.get('eventId');if(!(file instanceof File)||typeof eventId!=='string')return NextResponse.json({error:{code:'INVALID_INPUT',message:'이미지와 이벤트를 확인해주세요.'}},{status:400});if(file.type!=='image/webp'||file.size>2*1024*1024)return NextResponse.json({error:{code:'INVALID_INPUT',message:'2MB 이하의 WebP 이미지만 업로드할 수 있습니다.'}},{status:400});const path=`event-covers/${eventId}/${crypto.randomUUID()}.webp`;const admin=createAdminClient();const {error}=await admin.storage.from('event-covers').upload(path,file,{contentType:'image/webp',upsert:false});if(error)return NextResponse.json({error:{code:'INTERNAL_ERROR',message:error.message}},{status:500});const {data}=admin.storage.from('event-covers').getPublicUrl(path);return NextResponse.json({path,publicUrl:data.publicUrl},{status:201});}
